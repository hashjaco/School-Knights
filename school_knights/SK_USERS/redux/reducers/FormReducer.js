import {

} from "../actions/actionTypes";

let initialState = {

};

const formReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
